rename-session main

rename-window notes
send-keys "nvim note:elitebook"
send-keys ":vs note:sedimentum"

new-window -n dotfiles
send-keys "cd ~/git/dotfiles && nvim -S"
split-window -h
send-keys "cd ~/git/dotfiles"

new-window -n Infrastructure
send-keys "cd ~/git/sedimentum/Infrastructure && nvim -S"
split-window -h
send-keys "cd ~/git/sedimentum/Infrastructure"

new-session -t dev
rename-window db
new-window -n ml
new-window -n web

new-session -t staging
rename-window db
new-window -n ml
split-window -v
new-window -n web
split-window -v

new-session -t prod
rename-window db
new-window -n ml
split-window -v
new-window -n web
split-window -v

switch-client -t main

